---
layout: layouts/base.njk
title: Home
permalink: /
---

<div class="manual-home">
  <header class="manual-cover">
    <div class="manual-cover-band">TEM 1-01</div>
    <h1>The Engineer's Manual</h1>
    <p class="manual-cover-edition">{{ site.subtitle }}</p>
  </header>

  <nav class="manual-toc-full" aria-label="Table of contents">
    {% for chapter in collections.chapters %}
      {% if loop.first or chapter.data.part != collections.chapters[loop.index0 - 1].data.part %}
        {% if not loop.first %}</ul></section>{% endif %}
        <section class="manual-toc-part">
          <h2>Part {{ chapter.data.part }}: {{ chapter.data.partTitle }}</h2>
          <ul>
      {% endif %}
      <li>
        <a href="{{ chapter.url }}" class="manual-toc-link">
          <span class="manual-toc-num">{{ chapter.data.chapter }}</span>
          <span class="manual-toc-title">{{ chapter.data.title }}</span>
          {% if chapter.data.summary %}
            <span class="manual-toc-desc">{{ chapter.data.summary }}</span>
          {% endif %}
        </a>
      </li>
      {% if loop.last %}</ul></section>{% endif %}
    {% endfor %}

    {% if collections.appendices.length > 0 %}
      <section class="manual-toc-part">
        <h2>Appendices</h2>
        <ul>
          {% for appendix in collections.appendices %}
            <li>
              <a href="{{ appendix.url }}" class="manual-toc-link">
                <span class="manual-toc-title">{{ appendix.data.title }}</span>
              </a>
            </li>
          {% endfor %}
        </ul>
      </section>
    {% endif %}
  </nav>

  <footer class="manual-home-footer">
    <p><kbd class="mod-key">Ctrl</kbd>+<kbd>K</kbd> to search</p>
  </footer>
  <script>
    // Show Cmd on Mac, Ctrl elsewhere
    if (navigator.platform.indexOf('Mac') > -1) {
      document.querySelector('.mod-key').textContent = '⌘';
    }
  </script>
</div>
