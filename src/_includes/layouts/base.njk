<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{% if title %}{{ title }} | {% endif %}{{ site.title }}</title>
  <meta name="description" content="{{ description or site.description }}">
  <link rel="stylesheet" href="/assets/css/main.css">
  <link rel="stylesheet" href="/pagefind/pagefind-ui.css">
  <link rel="icon" href="/favicon.ico">
</head>
<body class="manual-body text-manual-800 min-h-screen flex flex-col font-serif">
  {% include "partials/header.njk" %}

  <main class="flex-1 manual-stage px-4 py-8 lg:py-12">
    <div class="manual-wrapper mx-auto max-w-6xl lg:flex lg:gap-8">
      <div class="manual-binder hidden lg:flex flex-col justify-around" aria-hidden="true">
        {% for _ in [1,2,3,4] %}
          <span class="manual-ring"></span>
        {% endfor %}
      </div>
      <div class="manual-page w-full">
        {{ content | safe }}
      </div>
    </div>
  </main>

  {% include "partials/footer.njk" %}

  <script src="/pagefind/pagefind-ui.js"></script>
  <script src="/assets/js/main.js"></script>
</body>
</html>
